# 카페24 상품페이지 → 리뷰 사이트 연결

> 리뷰 사이트(`review.zengenetics.co.kr`)를 만들어도 **카페24 상품페이지에서 링크가
> 없으면** 크롤러가 그 사이트를 발견할 경로가 없다. 이 문서는 그 연결 작업이다.

## 1. 상품페이지에 링크 추가 (스킨 수정)

**대상 파일:** 디자인 편집 → `moa/import/product_detail/review.html`
**작업용 디자인:** 젠제네틱스 v2 작업본 (`skin_no=16`) — **라이브(바디랩 반응형)는 직접 편집 금지**

### 붙여넣을 위치

9행(알파리뷰 위젯)과 10행(`alph_origin_board`) **사이**다.

```html
    <!-- 알파리뷰 - 상품 후기 영역 -->
    <div id="alph_new_board"><div class="alpha_widget" data-code="a070d8ba" data-value="{$product_no}" style="padding:10px;"></div></div>

    <!-- ↓↓↓ 여기에 붙인다 ↓↓↓ -->

    <div id="alph_origin_board" style="display:none">
```

### 붙여넣을 코드

```html
    <!-- 리뷰 전체 보기 — 서버렌더 리뷰 사이트로 가는 크롤 경로 -->
    <p class="review-allsite" style="text-align:center;margin:4px 0 28px;font-size:14.5px">
      <a href="https://review.zengenetics.co.kr/">젠제네틱스 구매 후기 전체 보기</a>
    </p>
```

### 왜 여기인가 — 10행 아래는 안 된다

10행이 여는 `<div id="alph_origin_board" style="display:none">` 는 161행까지 이어지고,
그 안의 카페24 원본 리뷰 게시판 마크업 전체가 **숨겨져 있다.** 구글은 `display:none`
안의 텍스트에 가중치를 주지 않는다. 그 안에 링크를 넣으면 효과가 깎인다.

(참고: 상품페이지에 읽을 리뷰 텍스트가 없는 이유가 둘이다 — ① 알파리뷰 위젯이
JS 로 주입해서 원본 HTML 이 비어 있고 ② 카페24 원본 게시판은 `display:none` 이다.)

### 왜 상품별 링크가 아니라 허브 링크인가

카페24 스킨은 `{$product_no}` 만 준다. 우리 리뷰 페이지는 slug 기반
(`potassium-1.html`)이라 번호에서 바로 조립할 수 없다. 허브(`/`)는 전 상품 리뷰
페이지를 모두 링크하므로 **크롤러는 2홉이면 전부 도달한다.** 상품별 딥링크가
필요하면 스킨에 product_no→slug 대응표를 넣어야 하는데, 그건 JS 가 되어 크롤
경로로는 쓸 수 없다. 정적 허브 링크가 맞다.

## 2. Search Console 에 서브도메인 등록

**서브도메인은 구글이 별개 사이트로 본다.** 쇼핑몰 속성에 자동으로 포함되지 않는다.

1. Search Console → 속성 추가 → **URL 접두어** → `https://review.zengenetics.co.kr/`
2. 소유 확인 — 쇼핑몰 루트에 이미 `google-site-verification` TXT 가 있으므로
   **DNS 확인**을 고르면 그 레코드로 통과될 가능성이 높다. 안 되면 TXT 를 하나 더 추가
3. 색인 생성 → **Sitemaps** → `https://review.zengenetics.co.kr/sitemap.xml` 제출

## 3. `reviewCount` 419 — 우리가 직접 못 고친다 (조사 결과)

상품페이지 JSON-LD 에 이렇게 박혀 있다:

```json
"aggregateRating": {"@type":"AggregateRating","ratingValue":4.8,"reviewCount":419}
```

실제 보유량은 **26,970건**이다. 구글은 실제의 1/64 로 안다.

**출처를 추적한 결과 카페24 코어가 서버에서 생성한다:**

| 확인 | 결과 |
|---|---|
| 스킨 파일에 `aggregateRating` | **0건** (`cafe24-skin/` 전수 검색) |
| 코드 직접입력 Head/Body 필드 | Organization 만 있고 Product 없음 |
| 라이브 HTML 내 위치 | `@79,574` — 코드 직접입력 블록(`@83,537~84,083`)보다 **앞** |
| 바로 앞 문맥 | 네이버 전환 스크립트(`wcs.trans`) — 카페24 렌더 영역 |
| JS 주입 여부 | 아님. `curl` 원본에 이미 있음 |

→ **스킨도 관리자 코드 필드도 아니다. 카페24가 만든다. 편집 경로가 없다.**

419 라는 숫자는 카페24 **상품후기 게시판**의 건수다. 알파리뷰 가이드가 그 게시판
데이터를 "임시용"이라고 명시하고 있고, 26,970건 중 대부분인 네이버페이 이관분은
카페24 게시판을 거치지 않았다. 그래서 영구히 어긋난다.

### 할 수 있는 것

1. **알파리뷰에 문의** — 카페24 게시판으로 전체 리뷰 수를 동기화해줄 수 있는지.
   이게 되면 419 가 실제값에 수렴한다. 유일한 근본 해결이다.
2. **우리 리뷰 사이트에 사실에 부합하는 `AggregateRating` 을 싣는다.**
   단 **화면에 보이는 리뷰로 뒷받침되는 수치만** 넣는다. 258건을 보여주면서
   26,970건이라고 선언하면 구글 구조화 데이터 정책 위반이다.
   → **엑셀 백필로 전량이 실제로 사이트에 올라간 뒤에 추가한다.**
3. 카페24에 상품 구조화 데이터 관련 설정이 있는지 관리자에서 확인 (있는지 미확인)

## 4. 심의 주의

1번 링크 문구는 `젠제네틱스 구매 후기 전체 보기` — **효능 표현이 0이다.** 의도적이다.
"붓기 후기 26,970건" 처럼 효능어나 건수를 강조하면 브랜드가 한 광고 주장이 되어
건강기능식품 표시·광고 사전심의 대상이 된다. 문구를 바꿀 때 검수를 거친다.
