# 생성 산출물. 워크플로가 매번 다시 만든다.
dist/
review-queue/
# 알파리뷰 엑셀 내보내기 원본 — 개인정보 컬럼이 섞여 들어올 수 있어 커밋하지 않는다.
# 스토어(data/*.json)에는 리뷰 본문·평점·작성일만 정규화해 들어간다.
*.csv
# 예외: 구글 시트가 IMPORTDATA 로 당겨가는 광고 일별 실적.
# 캠페인 단위 집계 수치(노출·클릭·비용)뿐이고 개인정보가 없다.
!data/ads_daily.csv
*.xlsx
__pycache__/
*.pyc
