# 젠제네틱스 리뷰/상세페이지 작업 규칙

이 저장소에서 작업할 때 반드시 지킬 것. 실제로 사고가 났던 항목만 적었다.

## 회사명

**(주)위스팟바이오랩** — 붙여 쓴다. 띄어쓰기 없음.

- 법인명 기준: 사이트 푸터 `상호명 : 주식회사 위스팟바이오랩`
- `(주)위스팟`, `(주)위스팟 바이오랩` 둘 다 틀렸다. 과거 파일에 남아 있던 표기다.
- 판매원 주소: `(주)위스팟바이오랩 · 서울특별시 강남구 강남대로 464, 416호`
- 제조원은 다른 회사다: `콜마비앤에이치(주) 세종 3공장`

## 카페24 상세설명에 넣는 코드

1. **항상 전체 교체 파일로 만든다.** "이미지 뒤부터 지우세요" 같은 부분 수정 지시를
   하면 안 된다. 칼륨 상세페이지 이미지 14개가 그렇게 날아갔다.

2. **`<img>` 태그를 새로 쓰지 않는다.** 라이브 HTML 에서 그대로 떠서 `alt` 만 끼운다.
   속성 하나만 달라도 이미지가 깨진다.

3. **`ec-data-src` 를 `src` 로 되돌린다.** 카페24 lazy-load 가 출력 시점에 바꿔놓은
   것이고, 관리자 편집기 기준은 `src` 다. 그대로 붙이면 이미지가 안 보인다.

4. **alt 재사용은 파일명이 아니라 내용(sha1) 기준.** `copy-<숫자>-` 접두사가 붙은
   파일은 기본 파일명이 같아도 **내용이 다르다**. 비타민B 데이팩 제품정보와 칼륨
   데이팩 제품정보가 그 경우다. 파일명으로 묶으면 엉뚱한 제품 정보가 alt 로 들어간다.

5. **`<script>` 에 의존하지 않는다.** 카페24가 상세설명의
   `<script type="application/ld+json">` 을 하룻밤 뒤 제거한다. 저장 직후에는 남아
   있다가 다음 날 사라졌다. 이미지·alt·`style`·`result`·`filesize` 같은 비표준
   속성까지 전부 그대로였고 `<script>` 만 없어졌다.
   → 구조화 데이터는 **마이크로데이터**(`itemscope`/`itemprop`)로 넣는다. `<script>` 는
   병행해서 같이 둔다.

6. **HTML 주석(`<!-- -->`)을 넣지 않는다.** 카페24 편집기가 주석을 깨뜨려서 내부 메모가
   라이브 페이지에 노출된 사고가 있었다.

7. **파일 끝이 잘렸는지 확인한다.** 붓기부스터 SET 이 마지막 `<script>` 블록만 잘려서
   붙여넣어졌다.

## 검증

- 라이브 확인은 **반드시 캐시 우회**(`Cache-Control: no-cache` + 쿼리스트링 난수).
  캐시된 응답을 보고 "이미지 14개 정상"이라고 두 번 잘못 보고한 적이 있다.
- 배포/붙여넣기 결과를 확인하지 않고 완료 보고하지 않는다. 워크플로가 실패해
  사이트가 258건에 멈춰 있는데 성공으로 보고한 적이 있다.
- `_watch.py` 가 매일 10개 상품 페이지의 이미지·alt·구조화 데이터·본문 글자수를 재고,
  지난 기준선보다 줄어들면 워크플로를 실패시킨다. 기준선은 `data/pagewatch.json`.

## 표시사항

- 식품유형이 제품마다 다르다. 칼륨 = **당류가공품**(일반식품), 마그네슘·비타민B =
  **건강기능식품**. 일반식품에 "건강기능식품이 아닙니다", 건강기능식품에 "건강기능식품
  입니다" 로 문구가 갈린다. 반대로 쓰면 사실과 다르다.
- 기능성 내용은 **포장 「영양·기능정보」 인쇄 문구 그대로** 쓴다. 상세페이지 이미지에는
  일부만 인쇄된 경우가 있다(비타민B6 의 호모시스테인 기능이 빠져 있었다).
- 영양정보는 **1포 기준**으로 통일한다. 고객이 총량을 1포 값으로 오해한다.
  칼륨 20포는 라벨이 총 내용량 기준이라 ÷20 환산이 필요하고, 마그네슘·비타민B 는
  라벨 값이 이미 1일 섭취량(1포) 값이다.
- 랭킹·수상 표기는 **측정 기준(매체·카테고리·기간)을 반드시 함께** 쓴다.

## 리뷰 데이터

- **리뷰번호가 없는 내보내기는 건수 근거로 쓰지 않는다.** 3컬럼(작성자ID·상품명·리뷰내용)
  51,711행 파일을 받고 "API 와 일치한다"며 26,913건으로 게시한 적이 있는데, 실제로는
  988개 문장이 반복된 것이었고 작성자ID 25,547행이 `네이버페이구매고객` 한 개의
  placeholder 였다. 리뷰번호가 있는 내보내기는 같은 1년치가 2,194건이다.
- 자체작업분이 섞인 데이터였고 이후 정리되었다. 위젯 API 에서도 해당 문장이 사라졌다.
- **게시는 (제품, 본문) 단위로 중복을 제거한다**(`_build.py` `dedupe`). 간편리뷰 프리셋
  때문에 서로 다른 고객이 같은 문장을 고르는 일이 많고, 같은 문장을 반복 게시하면
  크롤러에게 새 정보가 0 이면서 중복 콘텐츠가 된다. 원본은 스토어에 전부 남긴다.
- 엑셀 리뷰번호(`1008nJZbHNq`)와 위젯 API id(`78633090`)는 **다른 체계**다. id 로는
  같은 리뷰를 걸러낼 수 없으므로 본문으로 겹침을 막는다.
- `_build.py` 는 매번 `dist/` 를 비우고 짓는다. 남겨두면 스토어에서 빠진 리뷰가
  이전 빌드 페이지로 계속 배포된다.
